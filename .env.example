ANTHROPIC_API_KEY=your-api-key-here

# ElevenLabs: ConvAI voice in the web UI (WebSocket via signed URL).
# Get keys at https://elevenlabs.io/
ELEVENLABS_API_KEY=
# Agent ID from ElevenLabs Agents (ConvAI). Required for Voice On in the UI.
# Used both for signed URL minting and browser-side fallback startup.
ELEVENLABS_AGENT_ID=
# Optional: legacy HTTP STT/TTS routes (/api/voice/transcribe, /api/voice/speak)
# ELEVENLABS_VOICE_ID=
# ELEVENLABS_STT_MODEL_ID=scribe_v2
# ELEVENLABS_TTS_MODEL_ID=eleven_multilingual_v2
# ELEVENLABS_TTS_OUTPUT_FORMAT=mp3_44100_128
# ELEVENLABS_API_BASE=https://api.elevenlabs.io

# Optional: auth token for remote MCP endpoint /api/mcp/manual/ (for ElevenLabs MCP integration).
# If set, callers must send either:
#   Authorization: Bearer <token>
#   X-MCP-Auth-Token: <token>
# VOICE_MANUAL_MCP_TOKEN=
